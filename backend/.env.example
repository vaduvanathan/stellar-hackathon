# Walletsurance Backend
# Copy this file to .env and set your values (never commit .env)

# Required for deploy_contract.py (no need to type secret in terminal)
STELLAR_SECRET_KEY=SDZ...

# SQLite path (default: walletsurance.db in cwd)
# DATABASE_PATH=./walletsurance.db

# Stellar Soroban Testnet (for /api/contract/status and agent)
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
# Set after deploying: python deploy_contract.py
# CONTRACT_ID=...

# Optional: network passphrase (default: Testnet)
# NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Mock: no real keys required for hackathon
# AGENT_SECRET_KEY=...

PORT=8080
# FLASK_DEBUG=1

# Twilio SMS (for nominee claim links). Leave empty to mock (log only).
TWILIO_ACCOUNT_SID=your_account_sid
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_FROM_NUMBER=+1234567890

# Base URL for claim links in SMS (e.g. https://your-app.run.app)
# CLAIM_BASE_URL=https://your-app.run.app
